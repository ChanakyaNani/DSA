// hashing provides search, insert, delete in O(1) time. But we need to create the hash tables? all values are unique
// hash table is one of the most used data structured in CS after arrays

let obj = {
    1 : 'one',
    '2' : 'two'
};

console.log(obj['1']);